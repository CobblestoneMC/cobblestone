{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Cobblestone","text":"<ul> <li> <p>Players</p> <p>Save locations and navigate to them.</p> </li> <li> <p>Server admins</p> <p>Installation, commands, permissions, and configuration.</p> </li> <li> <p>Developers</p> <p>Register destinations, modify searches, and start trips.</p> </li> </ul>"},{"location":"#cobblestone","title":"Cobblestone","text":"<p>Server-side navigation for Minecraft.</p>"},{"location":"#overview","title":"Overview","text":"<p>Cobblestone runs an A* search over the live world and renders the resulting route as a particle trail. Routes account for walking, swimming, climbing, falling, boats, horses, doors, and portals. If the player leaves the route, it is recomputed.</p> <p>Cobblestone does not teleport players itself. Integrations can register teleport commands, such as EssentialsX <code>/home</code> or Towny <code>/town spawn</code>, as route steps that the player is prompted to run.</p> <pre><code>/stone location set home        # save the current position\n/nav home                       # navigate back to it\n</code></pre>"},{"location":"#architecture","title":"Architecture","text":"<pre><code>flowchart LR\n    A[\"/navigate &amp;lt;destination&amp;gt;\"] --&gt; B[Destination providers]\n    B --&gt; C[A* search]\n    D[Search modifications] --&gt; C\n    C --&gt; E[Path]\n    E --&gt; F[Trip]\n    F --&gt; G[Navigator]\n    G -. player strays .-&gt; C</code></pre> <p>Destination providers, search modifications, and navigators are all extensible. See the developer documentation.</p>"},{"location":"#supported-versions","title":"Supported versions","text":"Platform Minecraft Java Paper 26.1+ 25+ Sponge 1.21.1 21+ Sponge 26.1+ 25+"},{"location":"#performance","title":"Performance","text":"<p>Searches run asynchronously and are bounded per player. The configuration file exposes options to increase or reduce the resources each search may use.</p>"},{"location":"#metrics","title":"Metrics","text":"<p>Cobblestone reports anonymous, aggregate usage data to bStats. Disable it with <code>metrics.enabled: false</code>.</p>"},{"location":"#support","title":"Support","text":"<ul> <li>Issue tracker</li> <li>Releases and changelogs</li> <li>Source code</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>Cobblestone is MIT licensed. The full guide is CONTRIBUTING.md.</p>"},{"location":"contributing/#building","title":"Building","text":"<pre><code>git clone https://github.com/CobblestoneMC/cobblestone.git\ncd cobblestone/project\n./gradlew build\n</code></pre> <p>Requires JDK 21 and JDK 25. Core and Sponge modules target 21; Paper modules target 25.</p> <p>Before opening a pull request:</p> <pre><code>./gradlew spotlessApply     # format\n./gradlew build             # compile + test\n</code></pre>"},{"location":"contributing/#layout","title":"Layout","text":"Path What <code>project/api</code>, <code>project/core</code> Navigation API and A* engine <code>project/core-test</code> Tests against synthetic worlds <code>project/minecraft/plugin</code> Platform-independent plugin code <code>project/minecraft/platform/paper</code>, <code>\u2026/sponge*</code> Platform implementations <code>project/minecraft/integrations</code> Third-party integrations <code>project/examples/paper-warps</code> Example integration <code>docs/</code> This site"},{"location":"contributing/#bug-reports","title":"Bug reports","text":"<p>Open an issue with the server platform and version, the Cobblestone version, and relevant console output. For routing issues, include the search log line printed at <code>/cobblestone loglevel debug</code>.</p>"},{"location":"contributing/#documentation","title":"Documentation","text":"<pre><code>cd docs\npip install -r requirements.txt\nmkdocs serve\n</code></pre> <p>Pushes to <code>main</code> deploy the site. Changes to commands, permissions, configuration, or the API should update the corresponding page in the same pull request.</p>"},{"location":"players/","title":"Players","text":""},{"location":"players/#players","title":"Players","text":"<p>Cobblestone computes a route to a destination and draws it as a particle trail. No client modifications are required.</p> Command Alias Purpose <code>/navigate</code> <code>/nav</code> Navigate to a destination. <code>/cobblestone</code> <code>/stone</code> Manage saved locations and trips."},{"location":"players/#quick-start","title":"Quick start","text":"<p>Save your current position:</p> <pre><code>/stone location set home\n</code></pre> <p>Navigate back to it from anywhere:</p> <pre><code>/nav home\n</code></pre> <p>Cobblestone reports <code>Searching for a route...</code>, then <code>Success!</code> once a route is found. Hover over the message to see the search time and estimated travel time. The trip ends automatically when you arrive within two blocks of the destination.</p>"},{"location":"players/#the-trail","title":"The trail","text":"<p>The trail renders only the next segment of the route, brightest at the point to head for next.</p> <ul> <li>Straying. If you leave the trail, a short guide path is drawn back to it. Beyond 32 blocks   (by default), the route is recomputed from your current position.</li> <li>Action steps. Some steps require an action: opening a door, placing a boat, mounting a horse,   entering a portal, or running a command. Cobblestone highlights the location and sends a   message. Command prompts, such as <code>Run /warp market.</code>, can be clicked.</li> <li>Ending. A trip ends on arrival, on <code>/stone cancel</code>, or on disconnect. Death does not end a   trip.</li> </ul>"},{"location":"players/#destinations","title":"Destinations","text":"<p>Type <code>/nav</code> and press Tab to list the destinations available to you. The list depends on the plugins installed on the server.</p>"},{"location":"players/#saved-locations","title":"Saved locations","text":"<pre><code>/nav home                          # a personal location\n/nav location private home         # the same, fully qualified\n/nav location global spawn         # a server-wide location\n</code></pre>"},{"location":"players/#death-location","title":"Death location","text":"<pre><code>/nav death\n</code></pre> <p>Your most recent death location. Admins may disable this destination.</p>"},{"location":"players/#players_1","title":"Players","text":"<pre><code>/nav player Steve\n</code></pre> <p>Online players only. The route is recomputed periodically as the target moves.</p>"},{"location":"players/#worlds","title":"Worlds","text":"<pre><code>/nav world world_nether\n</code></pre> <p>Routes through a known entry point, typically a portal that Cobblestone has observed in use.</p>"},{"location":"players/#integrations","title":"Integrations","text":"<p>Available only when the corresponding integration is installed:</p> <pre><code>/nav town riverwood home           # Towny: town spawn\n/nav town riverwood outpost north  # Towny: outpost\n/nav npc Blacksmith                # Citizens: NPC\n/nav home cabin                    # EssentialsX: home\n/nav spawn                         # EssentialsX: spawn\n/nav quest lostlantern             # Quests, BeautyQuests: current objective\n/nav compass tower                 # BetonQuest: quest compass target\n</code></pre>"},{"location":"players/#addresses","title":"Addresses","text":"<p>Every destination has a full address, such as <code>cobblestone location private home</code>. Leading words may be omitted as long as the remainder identifies exactly one destination:</p> <pre><code>/nav cobblestone location private home\n/nav location home\n/nav home\n</code></pre> <p>The final word is always required. Some levels, such as town and NPC names, are marked strict by their provider and are also required.</p> <p>If the input matches more than one destination, the candidates are listed:</p> <pre><code>That destination is ambiguous; be more specific: location private home, essentials home home\n</code></pre> <p>Tab completion is suppressed when there are too many candidates. Type a few more characters and press Tab again.</p>"},{"location":"players/#flags","title":"Flags","text":"<p>Flags follow the destination and may be combined.</p> Flag Effect <code>-live</code> / <code>-no-live</code> Enable or disable periodic route recomputation. Enabled by default only for moving targets. <code>-no-&lt;mode&gt;</code> Exclude a travel mode, e.g. <code>-no-swim</code>. <code>-no-mode &lt;mode&gt;</code> Long form of the above. <code>-no-world &lt;world&gt;</code> Exclude a world, e.g. <code>minecraft:the_nether</code>. <code>-no-dimension &lt;dimension&gt;</code> Exclude a dimension, e.g. <code>the_end</code>. <code>-navigator &lt;id&gt;</code> Select a navigator. The default is <code>trail</code>. <p>Travel modes: <code>walk</code>, <code>swim</code>, <code>fly</code>, <code>mine</code>, <code>fall</code>, <code>climb</code>, <code>boat</code>, <code>horse</code>, <code>door</code>. The <code>fly</code> mode is used only if you are able to fly, and <code>mine</code> only where you may break blocks.</p> <pre><code>/nav home -no-swim -no-mine\n/nav player Steve -no-live\n/nav home -no-world minecraft:the_nether\n</code></pre>"},{"location":"players/#saved-locations_1","title":"Saved locations","text":"<pre><code>/stone location set &lt;name&gt;      # save the current position\n/stone location unset &lt;name&gt;    # delete a location\n/stone location list            # list personal and global locations\n</code></pre> <p><code>/stone loc</code> is an alias for <code>/stone location</code>. Names are single words and are scoped to each player. Where a personal and a global location share a name, use <code>location private &lt;name&gt;</code> or <code>location global &lt;name&gt;</code>.</p>"},{"location":"players/#trips","title":"Trips","text":"<p>Up to three trips may run at once by default.</p> <pre><code>/stone trips              # list active trips\n/stone cancel &lt;id&gt;        # cancel one trip\n/stone cancel all         # cancel all trips and pending searches\n</code></pre> <p><code>/stone cancel</code> with no argument is equivalent to <code>all</code>.</p>"},{"location":"players/#errors","title":"Errors","text":"Message Cause <code>There is no route to that destination.</code> No traversable route exists. <code>The search ran out of resources before finding a route.</code> The search reached its cell limit. <code>The search took too long and was stopped.</code> The search reached its time limit. <code>No destination matches ...</code> No destination has that name. <code>That destination is ambiguous; be more specific: ...</code> Add a word from the full address. <code>You already have too many active trips; cancel one first.</code> Cancel a trip with <code>/stone cancel</code>. <code>You don't have permission to do that.</code> The destination or navigator is restricted. <p>Search limits are set by the server. See Configuration.</p>"},{"location":"admins/","title":"Server admins","text":""},{"location":"admins/#server-admins","title":"Server admins","text":"<p>Cobblestone adds two commands, a set of permission nodes, and one configuration file.</p>"},{"location":"admins/#installation","title":"Installation","text":"PaperSponge <ol> <li>Download the Paper build from Modrinth.</li> <li>Place it in <code>plugins/</code>.</li> <li>Start the server.</li> </ol> <p>Requires Paper 26.1+ on Java 25+. Folia is supported.</p> <pre><code>plugins/cobblestone/\n  config.yml          # generated on first run\n  cobblestone.mv.db   # H2 database\n</code></pre> <ol> <li>Download the Sponge build from Modrinth.</li> <li>Place it in <code>mods/</code>.</li> <li>Start the server.</li> </ol> <p>Requires Sponge 1.21.1 on Java 21+, or Sponge 26.1+ on Java 25+.</p> <pre><code>config/cobblestone/\n  config.yml          # generated on first run\n  cobblestone.mv.db   # H2 database\n</code></pre> <p>No further setup is required. For large servers, review Configuration.</p>"},{"location":"admins/#features","title":"Features","text":""},{"location":"admins/#core","title":"Core","text":"<ul> <li>Personal and server-wide saved locations.</li> <li>Navigation to a player's death location, to other players, and to other worlds.</li> <li>Nether and End portal links, learned by observing players use them.</li> </ul> <pre><code>/stone location set home\n/stone location set spawn -global\n/nav death\n</code></pre>"},{"location":"admins/#integrations","title":"Integrations","text":"<p>Companion plugins add destinations, register teleport commands as route steps, and apply third-party build protection to routes.</p> Integration Adds Towny Towns, outposts, and plots; <code>/town spawn</code> and <code>/nation spawn</code> as route steps; build protection. EssentialsX Homes and spawn; <code>/home</code> and <code>/spawn</code> as route steps. Citizens NPCs. BetonQuest, BeautyQuests, Quests Quest objectives, with optional automatic trips. Typewriter A Navigate Player action."},{"location":"admins/#permissions","title":"Permissions","text":"<p>Player nodes default to allow; admin nodes default to op.</p> Node Default Grants <code>cobblestone.navigate</code> allow <code>/navigate</code>, <code>/cobblestone trips</code>, <code>/cobblestone cancel</code> <code>cobblestone.location</code> allow Personal locations <code>cobblestone.navigator</code> allow Non-default navigators <code>cobblestone.admin.location.global</code> op Global locations <code>cobblestone.admin.reload</code> op <code>/cobblestone reload</code> <code>cobblestone.admin.portals</code> op <code>/cobblestone portals clear</code> <code>cobblestone.admin.loglevel</code> op <code>/cobblestone loglevel</code> <p>Each destination also has its own node, derived from its address. For example, to prevent a group from navigating to other players:</p> <pre><code>/lp group default permission set cobblestone.navigate.cobblestone.player.* false\n</code></pre> <p>See Permissions.</p>"},{"location":"admins/#resource-usage","title":"Resource usage","text":""},{"location":"admins/#threading","title":"Threading","text":"<p>Pathfinding runs asynchronously. World reads and calls into other plugins, such as Towny build checks, run on the main thread. See <code>search.max_concurrent_per_player</code> and <code>search.algorithm.*</code> in Configuration.</p>"},{"location":"admins/#chunk-loading","title":"Chunk loading","text":"<p>By default, searches may load chunks that have already been generated. Terrain generation is disabled unless <code>search.chunks.policy</code> is set to <code>allow_load_and_generate</code>, which permanently enlarges world files. See Configuration.</p>"},{"location":"admins/#storage","title":"Storage","text":"<p>Saved locations, the most recent death location of each player, and learned portal links are stored in an embedded H2 database. Learned portal links can be cleared with <code>/cobblestone portals clear</code>.</p>"},{"location":"admins/commands/","title":"Commands","text":"<p>Cobblestone registers two commands. Both behave identically on Paper and Sponge.</p> Command Aliases <code>/navigate</code> <code>/nav</code> <code>/cobblestone</code> <code>/stone</code> (and <code>/cobblestone loc</code> for <code>location</code>)"},{"location":"admins/commands/#navigate-destination-flags","title":"<code>/navigate &lt;destination\u2026&gt; [flags]</code>","text":"<p>Permission: <code>cobblestone.navigate</code> (default allow) \u00b7 Players only</p> <p>Resolves the destination, runs a search, and starts a trip if a route is found. With no arguments, prints usage.</p>"},{"location":"admins/commands/#destination","title":"Destination","text":"<p>One or more words identifying a node in the destination tree. Each provider contributes a branch keyed by the name of the plugin that registered it:</p> <pre><code>cobblestone location private home\ncobblestone location global spawn\ncobblestone death\ncobblestone player &lt;name&gt;\ncobblestone world &lt;name&gt;\ntowny town &lt;town&gt; home\ncitizens npc &lt;name&gt;\nessentials home &lt;name&gt;\n</code></pre> <p>Leading words may be omitted if the remainder is unambiguous (<code>/nav home</code>). The final word and any level marked strict by its provider are always required. Ambiguous input is rejected with a list of candidates. Matching is case-insensitive.</p>"},{"location":"admins/commands/#flags","title":"Flags","text":"Flag Argument Effect <code>-navigator</code> navigator id Display style. Default <code>trail</code>. Gated by <code>cobblestone.navigator.&lt;id&gt;</code>. <code>-no-mode</code> mode word Exclude a travel mode from the search. <code>-no-&lt;mode&gt;</code> \u2014 Shorthand: <code>-no-fly</code>, <code>-no-boat</code>, <code>-no-mine</code>, \u2026 <code>-no-world</code> world key Exclude a world from routing. Repeatable. <code>-no-dimension</code> dimension Exclude a dimension from routing. Repeatable. <code>-live</code> \u2014 Force the trip to re-search periodically. <code>-no-live</code> \u2014 Force it not to. <p>Mode words: <code>walk</code>, <code>swim</code>, <code>fly</code>, <code>mine</code>, <code>fall</code>, <code>climb</code>, <code>boat</code>, <code>horse</code>, <code>door</code>.</p> <p>Trips to moving destinations (players) are live by default; all others are not. A live trip re-searches every <code>trips.live_interval_ticks</code> and yields to <code>search.max_concurrent_per_player</code>. A manual <code>/navigate</code> takes priority over a live re-search.</p>"},{"location":"admins/commands/#examples","title":"Examples","text":"<pre><code>/nav home\n/nav town riverwood home -no-mine\n/nav player Steve -no-live\n/nav location global spawn -no-dimension the_nether\n</code></pre>"},{"location":"admins/commands/#output","title":"Output","text":"Message Meaning <code>Searching for a route...</code> The search started. <code>Success!</code> A trip started. Hover for search time and estimated travel time. <code>There is no route to that destination.</code> <code>NO_ROUTE</code> or <code>DESTINATION_UNREACHABLE</code>. <code>The search ran out of resources before finding a route.</code> <code>search.algorithm.max_cells_visited</code> was hit. <code>The search took too long and was stopped.</code> <code>search.algorithm.max_wall_clock_seconds</code> was hit. <code>No destination matches {0}.</code> Nothing resolved. <code>That destination is ambiguous; be more specific: {0}</code> Several candidates. <code>You already have too many active trips; cancel one first.</code> <code>trips.max_active_per_player</code>. <code>Unknown navigator: {0}</code> No navigator registered under that id."},{"location":"admins/commands/#cobblestone","title":"<code>/cobblestone</code>","text":"<p>Prints help. All subcommands are also available under <code>/stone</code>.</p>"},{"location":"admins/commands/#cobblestone-location-set-name-global","title":"<code>/cobblestone location set &lt;name&gt; [-global]</code>","text":"<p>Permission: <code>cobblestone.location</code>; <code>-global</code> additionally needs <code>cobblestone.admin.location.global</code> \u00b7 Players only</p> <p>Saves the caller's current block position under <code>name</code>. Personal by default; <code>-global</code> makes it available to all players.</p> <pre><code>/stone location set home\n/stone location set spawn -global\n</code></pre> <p>Names are single words. Personal names are scoped per player. A personal and a global location may share a name; they are distinguished by address (<code>location private home</code>, <code>location global home</code>).</p>"},{"location":"admins/commands/#cobblestone-location-unset-name-global","title":"<code>/cobblestone location unset &lt;name&gt; [-global]</code>","text":"<p>Permission: <code>cobblestone.location</code>; <code>-global</code> needs <code>cobblestone.admin.location.global</code></p> <p>Removes a personal location, or a global location with <code>-global</code>.</p>"},{"location":"admins/commands/#cobblestone-location-list","title":"<code>/cobblestone location list</code>","text":"<p>Permission: <code>cobblestone.location</code> \u00b7 Players only</p> <p>Lists the caller's personal locations and all global locations.</p> <pre><code>Locations (3):\nhome - minecraft:overworld 128, 71, -344\nmine - minecraft:overworld 210, 12, -400\nspawn - minecraft:overworld 0, 64, 0 (global)\n</code></pre>"},{"location":"admins/commands/#cobblestone-trips","title":"<code>/cobblestone trips</code>","text":"<p>Permission: <code>cobblestone.navigate</code> \u00b7 Players only</p> <p>Lists the caller's active trips with id, destination, and estimated remaining time.</p> <pre><code>Active trips (2):\n[1] location private home - 2 minutes and 10 seconds\n[2] towny town riverwood home - 48 seconds\n</code></pre>"},{"location":"admins/commands/#cobblestone-cancel-idall","title":"<code>/cobblestone cancel [&lt;id&gt;|all]</code>","text":"<p>Permission: <code>cobblestone.navigate</code> \u00b7 Players only</p> <p>Cancels a trip by id. With <code>all</code> or no argument, cancels all trips and pending searches.</p>"},{"location":"admins/commands/#cobblestone-reload","title":"<code>/cobblestone reload</code>","text":"<p>Permission: <code>cobblestone.admin.reload</code></p> <p>Reloads <code>config.yml</code>. Live settings apply immediately. Changes to restart-only settings are reported and not applied:</p> <pre><code>Cobblestone configuration reloaded.\nSome settings changed but need a server restart to apply: data.backend, search.chunks.policy\n</code></pre>"},{"location":"admins/commands/#cobblestone-portals-clear","title":"<code>/cobblestone portals clear</code>","text":"<p>Permission: <code>cobblestone.admin.portals</code></p> <p>Deletes all learned portal links. Links are relearned as players use portals.</p> <pre><code>Cleared 42 discovered portal transitions.\n</code></pre>"},{"location":"admins/commands/#cobblestone-loglevel-level","title":"<code>/cobblestone loglevel &lt;level&gt;</code>","text":"<p>Permission: <code>cobblestone.admin.loglevel</code></p> <p>Sets Cobblestone's console log level until restart: <code>trace</code>, <code>debug</code>, <code>info</code>, <code>warn</code>, or <code>error</code>.</p> <p>At <code>debug</code>, each search logs its outcome, step count, and timing.</p>"},{"location":"admins/config/","title":"Configuration","text":"<p><code>config.yml</code> is generated on first run, with a comment above each setting.</p> PaperSponge <p><code>plugins/cobblestone/config.yml</code></p> <p><code>config/cobblestone/config.yml</code></p> <p><code>/cobblestone reload</code> reloads the file. Settings marked live apply immediately; settings marked restart apply on the next server start.</p> <p>Each platform generates its own settings. Unrecognized settings are reported on startup and ignored.</p>"},{"location":"admins/config/#locale","title":"<code>locale</code>","text":"Setting Default <code>locale.default</code> <code>en</code> restart <p>Language for console and system messages, as a BCP 47 tag (<code>en</code>, <code>en-US</code>, <code>fr</code>). Player messages use the client's language when a translation is available.</p>"},{"location":"admins/config/#messages","title":"<code>messages</code>","text":"Setting Default <code>messages.show_prefix</code> <code>true</code> live <p>Prepends the <code>[\u2726]</code> prefix to player messages.</p>"},{"location":"admins/config/#data","title":"<code>data</code>","text":"Setting Default Values <code>data.backend</code> <code>H2</code> <code>H2</code> restart <code>data.file</code> <code>cobblestone</code> any name restart <p>Storage for saved locations, death locations, and portal links. <code>data.file</code> is the database file name, without extension, in the plugin's data folder.</p>"},{"location":"admins/config/#trips","title":"<code>trips</code>","text":"Setting Default <code>trips.max_active_per_player</code> <code>3</code> live <code>trips.live_interval_ticks</code> <code>100</code> live <code>trips.recalculate_distance</code> <code>32</code> live <ul> <li><code>max_active_per_player</code>: Maximum concurrent trips per player.</li> <li><code>live_interval_ticks</code>: Interval between re-searches for live trips. Live re-searches are the   main recurring cost; increase this before reducing search limits.</li> <li><code>recalculate_distance</code>: Distance in blocks a player may stray before the route is   recomputed. Below this distance, a guide path back to the trail is drawn instead. <code>0</code> disables   recomputation.</li> </ul>"},{"location":"admins/config/#search","title":"<code>search</code>","text":"Setting Default <code>search.max_concurrent_per_player</code> <code>1</code> restart <p>Maximum concurrent searches per player, including live re-searches. Live re-searches yield to this limit; a manual <code>/navigate</code> always runs.</p>"},{"location":"admins/config/#searchalgorithm","title":"<code>search.algorithm</code>","text":"Setting Default <code>search.algorithm.max_cells_visited</code> <code>200000</code> live <code>search.algorithm.max_wall_clock_seconds</code> <code>60</code> live <code>search.algorithm.heuristic_weight</code> <code>1.5</code> live <code>search.algorithm.tier1_unsolved_pessimism</code> <code>1.5</code> live <code>search.algorithm.running_average_width</code> <code>5</code> live <ul> <li><code>max_cells_visited</code>: Cell limit per search. Memory use is a few hundred bytes per cell, so the   default peaks in the tens of megabytes. Increase for longer routes; decrease on small heaps.</li> <li><code>max_wall_clock_seconds</code>: Time limit per search.</li> <li><code>heuristic_weight</code>: A* heuristic weight. <code>1.0</code> yields optimal routes; higher values search   faster with routes at most this factor longer than optimal.</li> <li><code>tier1_unsolved_pessimism</code>: Cost multiplier for route legs that have not yet been solved.   <code>1.0</code> disables it.</li> <li><code>running_average_width</code>: Window size of the running-average heuristic.</li> </ul>"},{"location":"admins/config/#searchchunks","title":"<code>search.chunks</code>","text":"Setting Default Values <code>search.chunks.policy</code> <code>allow_load</code> <code>loaded_only</code>, <code>allow_load</code>, <code>allow_load_and_generate</code> restart <ul> <li><code>loaded_only</code>: Read only chunks already in memory. Lowest cost; searches frequently fail   where terrain is unloaded.</li> <li><code>allow_load</code>: Also read generated chunks from disk. Recommended.</li> <li><code>allow_load_and_generate</code>: Also generate new terrain. World files grow permanently.</li> </ul> PaperSponge <p>Paper can read an unloaded chunk without loading it, so <code>allow_load</code> is inexpensive.</p> <p>Sponge must load a chunk to read it. Cobblestone acquires a chunk ticket, copies the chunk, and releases the ticket immediately.</p> Setting Default <code>search.chunks.max_load_requests</code> <code>256</code> live <p>Maximum chunk tickets held at once. Values below about 64 disable read-ahead and slow searches.</p>"},{"location":"admins/config/#tuning","title":"Tuning","text":"Symptom Adjustment Long routes fail with \"ran out of resources\" or \"took too long\" Increase <code>max_cells_visited</code> and <code>max_wall_clock_seconds</code>. Ensure <code>search.chunks.policy</code> is at least <code>allow_load</code>. High CPU usage Increase <code>trips.live_interval_ticks</code>, keep <code>search.max_concurrent_per_player</code> at <code>1</code>, and decrease <code>max_cells_visited</code>. On Sponge, decrease <code>search.chunks.max_load_requests</code>. High memory usage Decrease <code>max_cells_visited</code>. Searches must not load chunks Set <code>search.chunks.policy</code> to <code>loaded_only</code>. <p>Use <code>/cobblestone loglevel debug</code> to log the timing of each search.</p>"},{"location":"admins/config/#portals","title":"<code>portals</code>","text":"<p>Portal destinations are not exposed by the server API, so Cobblestone learns portal links by observing players.</p> Setting Default <code>portals.discovery</code> <code>true</code> live <code>portals.cost_seconds</code> <code>5.0</code> live <code>portals.normalize_entry</code> <code>true</code> live <code>portals.normalize_exit</code> <code>true</code> live <ul> <li><code>discovery</code>: Learn new portal links. Existing links remain in use when disabled.</li> <li><code>cost_seconds</code>: Assumed cost of a portal traversal.</li> <li><code>normalize_entry</code>: Treat any entry into a nether portal as entering at its center, so each   portal has a single destination. Required for reliable portal routing.</li> <li><code>normalize_exit</code>: Place the player at the center of the destination portal, at ground level.</li> </ul> <p>Clear learned links with <code>/cobblestone portals clear</code>.</p>"},{"location":"admins/config/#deaths","title":"<code>deaths</code>","text":"Setting Default <code>deaths.track</code> <code>true</code> live <p>Records each player's most recent death location and offers the <code>death</code> destination. When disabled, recording stops and the destination is hidden; stored data is retained. To restrict it by group instead, use <code>cobblestone.navigate.cobblestone.death</code>.</p>"},{"location":"admins/config/#metrics","title":"<code>metrics</code>","text":"Setting Default <code>metrics.enabled</code> <code>true</code> restart <p>Anonymous usage data via bStats. bStats can also be disabled globally in its own configuration.</p>"},{"location":"admins/config/#navigatorstrail","title":"<code>navigators.trail</code>","text":"Setting Default <code>navigators.trail.buffer_cells</code> <code>256</code> live <code>navigators.trail.particles</code> <code>[SCRAPE, WAX_OFF]</code> live <code>navigators.trail.highlight_particles</code> <code>[END_ROD]</code> live <code>navigators.trail.colors</code> <code>[55FFFF, FFAA00, FFFFFF]</code> live <code>navigators.trail.density</code> <code>0.2</code> live <ul> <li><code>buffer_cells</code>: Length of the rendered path segment ahead of the player.</li> <li><code>particles</code>: Particle types, chosen at random per particle. <code>DUST</code> uses <code>colors</code>; other types   render as-is. Particles requiring data other than a color are not supported.</li> <li><code>highlight_particles</code>: Particles at highlighted points, such as action steps.</li> <li><code>colors</code>: <code>DUST</code> colors as <code>RRGGBB</code>, chosen at random per particle.</li> <li><code>density</code>: Mean particles per cell per tick. Fractional values are probabilities: <code>0.7</code> means   a 70% chance of one particle per cell per tick.</li> </ul> <p>Particle names follow the server's particle registry.</p>"},{"location":"admins/integrations/","title":"Integrations","text":"<p>Each integration is a separate plugin, available on Modrinth. An integration requires both Cobblestone and its target plugin, and disables itself if the target is absent.</p> <p>The integrations listed here are Paper builds.</p> <p>Integration destinations are registered under the target plugin's name. Permission nodes therefore take the form <code>cobblestone.navigate.towny.\u2026</code>, not <code>cobblestone.navigate.cobblestonetowny.\u2026</code>. See Permissions.</p>"},{"location":"admins/integrations/#towny","title":"Towny","text":"<p>CobblestoneTowny \u00b7 requires Towny</p> <pre><code>/nav town &lt;town&gt; home            # town spawn\n/nav town &lt;town&gt; outpost &lt;name&gt;\n/nav town &lt;town&gt; plot &lt;name&gt;     # named plot\n/nav town &lt;town&gt; type &lt;name&gt;     # plots by type (shop, farm, \u2026)\n/nav resident                    # the player's own town\n</code></pre> <p>The <code>town</code> level is strict: <code>/nav town riverwood home</code> is valid; <code>/nav riverwood home</code> is not.</p> <ul> <li>Teleport route steps. <code>/town spawn</code>, <code>/nation spawn</code>, and <code>/town outpost</code> are offered as route   steps when the player is permitted to use them under Towny's rules.</li> <li>Build protection. Routes do not mine through blocks the player may not break.</li> </ul>"},{"location":"admins/integrations/#essentialsx","title":"EssentialsX","text":"<p>CobblestoneEssentials \u00b7 requires EssentialsX; EssentialsSpawn optional</p> <pre><code>/nav home &lt;name&gt;     # an Essentials home\n/nav spawn           # server spawn (requires EssentialsSpawn)\n</code></pre> <p><code>/home</code> and <code>/spawn</code> are offered as route steps when the player is permitted to use them.</p>"},{"location":"admins/integrations/#citizens","title":"Citizens","text":"<p>CobblestoneCitizens \u00b7 requires Citizens</p> <pre><code>/nav npc &lt;name&gt;\n</code></pre> <p>NPC positions are resolved at search time. The <code>npc</code> level is strict.</p>"},{"location":"admins/integrations/#betonquest","title":"BetonQuest","text":"<p>CobblestoneBetonQuest \u00b7 requires BetonQuest \u00b7 GPL-3.0</p> <pre><code>/nav compass &lt;name&gt;\n</code></pre> <p>Each active quest compass is a destination. Setting a compass target can start a trip automatically.</p> <p><code>plugins/CobblestoneBetonQuest/config.yml</code>:</p> <pre><code>auto-navigate: true      # start a trip when a compass target is set\nnavigator: trail\nparticles: [DUST, GLOW]\ncolors: [FFD54F, 4FC3F7]\ncompasses:               # per-compass overrides, keyed by compass id\n  dungeon_entrance:\n    enabled: false\n</code></pre> <p>This module links against BetonQuest and is licensed under GPL-3.0.</p>"},{"location":"admins/integrations/#beautyquests","title":"BeautyQuests","text":"<p>CobblestoneBeautyQuests \u00b7 requires BeautyQuests</p> <pre><code>/nav quest &lt;name&gt;\n</code></pre> <p>The current locatable stage of each started quest is a destination. Advancing to a locatable stage can start a trip automatically.</p> <p><code>plugins/CobblestoneBeautyQuests/config.yml</code> (per-quest overrides keyed by numeric quest id):</p> <pre><code>auto-navigate: true\nnavigator: trail\nquests:\n  '3':\n    enabled: false\n</code></pre>"},{"location":"admins/integrations/#quests-pikamug","title":"Quests (PikaMug)","text":"<p>CobblestonePikamugQuests \u00b7 requires Quests</p> <pre><code>/nav quest &lt;name&gt;\n</code></pre> <p>Current objectives are destinations, with the same automatic trip behavior. Per-quest overrides are keyed by quest id:</p> <pre><code>quests:\n  slay-the-dragon:\n    enabled: false\n</code></pre> <p>The plugin is named <code>Quests</code>, so its nodes take the form <code>cobblestone.navigate.quests.quest.&lt;name&gt;</code>.</p>"},{"location":"admins/integrations/#typewriter","title":"Typewriter","text":"<p>cobblestone-typewriter \u00b7 Typewriter extension</p> <p>Adds a Navigate Player action entry, which starts a trip for the triggering player to a configured location. Install it as a Typewriter extension, not in <code>plugins/</code>.</p>"},{"location":"admins/integrations/#name-collisions","title":"Name collisions","text":"<p>Multiple integrations may offer the same name, such as <code>home</code>. Destinations remain distinct by full address (<code>essentials home cabin</code>, <code>towny town riverwood home</code>). Ambiguous player input is rejected with a list of candidates.</p>"},{"location":"admins/integrations/#custom-integrations","title":"Custom integrations","text":"<p>All integration functionality is available through the public API. See the developer documentation.</p>"},{"location":"admins/permissions/","title":"Permissions","text":"<p>Cobblestone defines a fixed set of command nodes and generates a node for every destination and navigator.</p>"},{"location":"admins/permissions/#command-nodes","title":"Command nodes","text":"Node Default Grants <code>cobblestone.navigate</code> allow <code>/navigate</code>, <code>/cobblestone trips</code>, <code>/cobblestone cancel</code> <code>cobblestone.location</code> allow <code>/cobblestone location set/unset/list</code> for personal locations <code>cobblestone.navigator</code> allow Parent of the per-navigator nodes <code>cobblestone.admin.location.global</code> op Creating and deleting global locations <code>cobblestone.admin.reload</code> op <code>/cobblestone reload</code> <code>cobblestone.admin.portals</code> op <code>/cobblestone portals clear</code> <code>cobblestone.admin.loglevel</code> op <code>/cobblestone loglevel</code> PaperSponge <p>Declared in <code>paper-plugin.yml</code> with the defaults above.</p> <p>Registered with the permission service on startup. Player nodes are assigned to the <code>USER</code> role and admin nodes to the <code>ADMIN</code> role.</p>"},{"location":"admins/permissions/#destination-nodes","title":"Destination nodes","text":"<p>Each destination has a canonical address, which maps to a node under <code>cobblestone.navigate</code>:</p> Address Node <code>cobblestone location private home</code> <code>cobblestone.navigate.cobblestone.location.private.home</code> <code>cobblestone death</code> <code>cobblestone.navigate.cobblestone.death</code> <code>cobblestone player Steve</code> <code>cobblestone.navigate.cobblestone.player.Steve</code> <code>towny town riverwood home</code> <code>cobblestone.navigate.towny.town.riverwood.home</code> <code>citizens npc Blacksmith</code> <code>cobblestone.navigate.citizens.npc.Blacksmith</code> <p>The first segment is the lower-cased name of the owning plugin. Integrations register under the plugin they integrate with (<code>towny</code>, <code>citizens</code>, <code>essentials</code>, <code>betonquest</code>, \u2026), not under the name of the companion plugin.</p>"},{"location":"admins/permissions/#evaluation","title":"Evaluation","text":"<ul> <li>Default allow. A destination is permitted unless its node is explicitly set to <code>false</code>.</li> <li> <p>Exact node only. Parent nodes are not consulted, so denying <code>cobblestone.navigate.towny</code> has   no effect. Deny a branch with a wildcard that your permission plugin expands:</p> <pre><code>/lp group default permission set cobblestone.navigate.towny.* false\n</code></pre> <p>The permission plugin's own precedence rules apply, so a narrower grant overrides a broader deny.</p> </li> <li> <p>Tab completion. Denied destinations are omitted from suggestions.</p> </li> </ul>"},{"location":"admins/permissions/#examples","title":"Examples","text":"<p>Deny navigation to players:</p> <pre><code>/lp group default permission set cobblestone.navigate.cobblestone.player.* false\n</code></pre> <p>Deny the death destination except for one group:</p> <pre><code>/lp group default permission set cobblestone.navigate.cobblestone.death false\n/lp group donor permission set cobblestone.navigate.cobblestone.death true\n</code></pre> <p>Deny a single town:</p> <pre><code>/lp group default permission set cobblestone.navigate.towny.town.riverwood.* false\n</code></pre> <p>Deny all Citizens NPCs except one:</p> <pre><code>/lp group default permission set cobblestone.navigate.citizens.* false\n/lp group default permission set cobblestone.navigate.citizens.npc.Blacksmith true\n</code></pre>"},{"location":"admins/permissions/#navigation-and-teleportation","title":"Navigation and teleportation","text":"<p>Destination nodes control only whether a player may be routed to a destination. Teleport commands such as <code>/home</code>, <code>/town spawn</code>, and <code>/warp</code> remain governed by their own plugins' permissions. Integrations offer a teleport as a route step only when the player may run the command.</p>"},{"location":"admins/permissions/#provider-permissions","title":"Provider permissions","text":"<p>A provider may attach additional required permissions to a destination (<code>MinecraftDestination#permissions</code>). The player must hold all of them. Cobblestone's own destinations declare none.</p>"},{"location":"admins/permissions/#navigator-nodes","title":"Navigator nodes","text":"<p>Non-default navigators are gated by <code>cobblestone.navigator.&lt;id&gt;</code>:</p> <pre><code>/lp group default permission set cobblestone.navigator.guide false\n</code></pre> <p>These nodes default to allow and are checked only when <code>-navigator &lt;id&gt;</code> is used. The default <code>trail</code> navigator requires only <code>cobblestone.navigate</code>.</p>"},{"location":"admins/permissions/#case-sensitivity","title":"Case sensitivity","text":"<p>Destination keys are used verbatim as node segments (<code>\u2026citizens.npc.Blacksmith</code>). If a deny has no effect, try the lower-cased form; not all permission plugins fold case consistently.</p>"},{"location":"developers/","title":"Developers","text":""},{"location":"developers/#developers","title":"Developers","text":"Task Page Register destinations Destinations Start a trip for a player Trips Add teleports and other transitions to searches Search modification Restrict where searches may break or pass Search modification Render routes with a custom navigator Navigators Run a search and read the path Searching <p>Platform-specific code blocks are tabbed. Selecting Paper or Sponge applies site-wide.</p>"},{"location":"developers/#dependency","title":"Dependency","text":"<p>Two artifacts are published per platform:</p> <ul> <li><code>\u2026-api</code>: The navigation library: searches and search modifications.</li> <li><code>\u2026-plugin-api</code>: Plugin features: destinations, navigators, and trips. Includes <code>\u2026-api</code>   transitively.</li> </ul> <p>Most integrations should depend on <code>\u2026-plugin-api</code>.</p> PaperSponge build.gradle.kts<pre><code>repositories {\n    mavenCentral()\n}\n\ndependencies {\n    // Provided at runtime by the Cobblestone plugin \u2014 never shade it.\n    compileOnly(\"org.cobblestonemc:paper-plugin-api:0.1.0\")\n}\n</code></pre> pom.xml<pre><code>&lt;dependency&gt;\n  &lt;groupId&gt;org.cobblestonemc&lt;/groupId&gt;\n  &lt;artifactId&gt;paper-plugin-api&lt;/artifactId&gt;\n  &lt;version&gt;0.1.0&lt;/version&gt;\n  &lt;scope&gt;provided&lt;/scope&gt;\n&lt;/dependency&gt;\n</code></pre> <p>Declare the dependency so that Cobblestone loads first:</p> paper-plugin.yml<pre><code>dependencies:\n  server:\n    cobblestone:\n      load: BEFORE\n</code></pre> build.gradle.kts<pre><code>repositories {\n    mavenCentral()\n}\n\ndependencies {\n    // Provided at runtime by the Cobblestone plugin \u2014 never shade it.\n    compileOnly(\"org.cobblestonemc:sponge-plugin-api:0.1.0\")\n}\n</code></pre> pom.xml<pre><code>&lt;dependency&gt;\n  &lt;groupId&gt;org.cobblestonemc&lt;/groupId&gt;\n  &lt;artifactId&gt;sponge-plugin-api&lt;/artifactId&gt;\n  &lt;version&gt;0.1.0&lt;/version&gt;\n  &lt;scope&gt;provided&lt;/scope&gt;\n&lt;/dependency&gt;\n</code></pre> <p>Declare the dependency so that Cobblestone loads first:</p> META-INF/sponge_plugins.json<pre><code>\"dependencies\": [\n  { \"id\": \"cobblestone\", \"load-order\": \"after\", \"optional\": false }\n]\n</code></pre> <p>Do not shade the API</p> <p>The API is provided at runtime by the Cobblestone plugin. A shaded copy causes <code>ClassCastException</code>s across class loaders.</p> <p>Latest version: . The API is pre-1.0 and may change between minor versions.</p>"},{"location":"developers/#services","title":"Services","text":"<p>Services are accessed through two static entry points:</p> PaperSponge <pre><code>import org.cobblestonemc.paper.api.CobblestoneCoreApi;\nimport org.cobblestonemc.paper.plugin.api.CobblestonePaperApi;\n\n// \u2026-api: the navigation library\nNavigationService navigation = CobblestoneCoreApi.navigationService();\nSearchModificationRegistrar searches = CobblestoneCoreApi.registrar();\n\n// \u2026-plugin-api: destinations, navigators, trips\nIntegrationRegistrar integrations = CobblestonePaperApi.registrar();\nTripService trips = CobblestonePaperApi.tripService();\n</code></pre> <p>Both query Bukkit's <code>ServicesManager</code>. Call them from <code>onEnable()</code> or later, not from a constructor.</p> <pre><code>import org.cobblestonemc.sponge.api.CobblestoneCoreApi;\nimport org.cobblestonemc.sponge.plugin.api.CobblestoneSpongeApi;\n\n// \u2026-api: the navigation library\nNavigationService navigation = CobblestoneCoreApi.navigationService();\nSearchModificationRegistrar searches = CobblestoneCoreApi.registrar();\n\n// \u2026-plugin-api: destinations, navigators, trips\nIntegrationRegistrar integrations = CobblestoneSpongeApi.registrar();\nTripService trips = CobblestoneSpongeApi.tripService();\n</code></pre> <p>Cobblestone populates these holders during its <code>ConstructPluginEvent</code>. Access them from <code>StartingEngineEvent</code> or later.</p> <p>Naming</p> <p>The Paper accessor is <code>CobblestonePaperApi</code>; the Sponge accessor is <code>CobblestoneSpongeApi</code>. The names will be aligned in a future major version.</p>"},{"location":"developers/#ownership","title":"Ownership","text":"<p>Every registration has an owner plugin. Registrations are removed when the owner disables. The owner's name also determines the destination branch and its permission nodes.</p> PaperSponge <pre><code>CobblestonePaperApi.registrar().registerDestinations(this, new MyDestinations());\n</code></pre> <p>Passing <code>this</code> registers destinations under your plugin's lower-cased name.</p> <p>An integration for another plugin should pass that plugin, so that the address is <code>town riverwood home</code> rather than <code>cobblestonetowny town riverwood home</code>:</p> <pre><code>Plugin towny = getServer().getPluginManager().getPlugin(\"Towny\");\nCobblestonePaperApi.registrar().registerDestinations(towny, new TownyDestinations());\n</code></pre> <pre><code>CobblestoneSpongeApi.registrar().registerDestinations(container, new MyDestinations());\n</code></pre> <p>Passing your <code>PluginContainer</code> registers destinations under your plugin's lower-cased id.</p> <p>An integration for another plugin should pass that plugin's container, so that the address is <code>town riverwood home</code> rather than <code>cobblestonetowny town riverwood home</code>:</p> <pre><code>PluginContainer towny = Sponge.pluginManager().plugin(\"towny\").orElseThrow();\nCobblestoneSpongeApi.registrar().registerDestinations(towny, new TownyDestinations());\n</code></pre>"},{"location":"developers/#architecture","title":"Architecture","text":"<pre><code>flowchart TB\n    subgraph you[\"Your plugin\"]\n        DS[DestinationService]\n        SMS[SearchModificationService]\n        NF[NavigatorFactory]\n        CALL[Direct call]\n    end\n\n    DS --&gt;|IntegrationRegistrar| NAV[\"/navigate\"]\n    NF --&gt;|IntegrationRegistrar| TRIP[Trip]\n    SMS --&gt;|SearchModificationRegistrar| SEARCH[Search]\n    NAV --&gt; SEARCH\n    SEARCH --&gt;|Path| TRIP\n    CALL --&gt;|TripService| TRIP\n    CALL --&gt;|NavigationService| SEARCH</code></pre> <ul> <li>Destination: A named location that players can navigate to.</li> <li>Search: Computes a <code>Path</code> from a player to a destination.</li> <li>Search modification: Adds transitions and restricts which blocks may be broken or entered.</li> <li>Trip: A path being followed by a player, ticked by Cobblestone.</li> <li>Navigator: Renders a trip.</li> </ul>"},{"location":"developers/#threading","title":"Threading","text":"<ul> <li><code>SearchHandle.future()</code> completes on a Cobblestone thread. Return to the main thread (or region   thread on Folia) before accessing server state.</li> <li><code>computeTransitions</code>, <code>computeBreakChecker</code>, and <code>computePassChecker</code> run once per search on the   calling thread, normally the main thread.</li> <li>The returned checkers run during the search, possibly off the main thread, and return a   <code>CompletableFuture</code>. Checks that require the main thread must schedule work there and complete   the future.</li> <li>Checkers are invoked thousands of times per search. Return completed futures where possible and   cache results per search.</li> </ul>"},{"location":"developers/destinations/","title":"Destinations","text":"<p>A destination is a named location that <code>/navigate</code> can route to. Destinations are supplied by a <code>DestinationService</code>, which is queried per player, per search.</p>"},{"location":"developers/destinations/#structure","title":"Structure","text":"<pre><code>flowchart LR\n    A[DestinationService] --&gt;|provide player| B[DestinationTree]\n    B --&gt; C[sub-trees]\n    B --&gt; D[leaves]\n    C --&gt; B\n    D --&gt; E[MinecraftDestination]\n    E --&gt; F[\"regions() \u2014 where it actually is\"]</code></pre>"},{"location":"developers/destinations/#minimal-example","title":"Minimal example","text":"<p>A single destination at a fixed location:</p> PaperSponge <pre><code>import org.cobblestonemc.paper.plugin.api.CobblestonePaperApi;\nimport org.cobblestonemc.paper.plugin.api.Destination;\nimport org.cobblestonemc.paper.plugin.api.DestinationService;\nimport org.cobblestonemc.paper.plugin.api.DestinationTree;\n\npublic final class ShopsPlugin extends JavaPlugin {\n  @Override\n  public void onEnable() {\n    CobblestonePaperApi.registrar().registerDestinations(this, player -&gt;\n        DestinationTree.builder()\n            .leaf(\"market\", () -&gt; Destination.at(marketLocation(), \"Market\"))\n            .build());\n  }\n}\n</code></pre> <pre><code>import net.kyori.adventure.text.Component;\nimport org.cobblestonemc.sponge.plugin.api.CobblestoneSpongeApi;\nimport org.cobblestonemc.sponge.plugin.api.Destination;\nimport org.cobblestonemc.sponge.plugin.api.DestinationService;\nimport org.cobblestonemc.sponge.plugin.api.DestinationTree;\n\n@Listener\npublic void onStartedEngine(StartedEngineEvent&lt;Server&gt; event) {\n  CobblestoneSpongeApi.registrar().registerDestinations(container, player -&gt;\n      DestinationTree.builder()\n          .leaf(\"market\", () -&gt; Destination.at(marketLocation(), Component.text(\"Market\")))\n          .build());\n}\n</code></pre> <p>For a plugin named <code>Shops</code>, the address is <code>shops market</code>. Players may use <code>/nav market</code>, or <code>/nav shops market</code> if the name is ambiguous.</p> <p>Note</p> <p>The tree is already registered under the owner plugin's name. Do not add a top-level node for it.</p>"},{"location":"developers/destinations/#trees","title":"Trees","text":"<p><code>DestinationTree.builder()</code> creates leaves and subtrees, each keyed by a string:</p> PaperSponge <pre><code>DestinationTree.builder()\n    .subtree(\"warp\", () -&gt; warps(player))\n    .subtree(\"dungeon\", DestinationTree.builder()\n        .strict()                                     // (1)!\n        .leaf(\"crypt\", () -&gt; Destination.at(crypt, \"The Crypt\"))\n        .leaf(\"mines\", () -&gt; Destination.at(mines, \"Deep Mines\"))\n        .build())\n    .build();\n</code></pre> <ol> <li>A strict level cannot be omitted: <code>/nav dungeon crypt</code> is valid; <code>/nav crypt</code> is not.</li> </ol> <pre><code>DestinationTree.builder()\n    .subtree(\"warp\", () -&gt; warps(player))\n    .subtree(\"dungeon\", DestinationTree.builder()\n        .strict()                                     // (1)!\n        .leaf(\"crypt\", () -&gt; Destination.at(crypt, Component.text(\"The Crypt\")))\n        .leaf(\"mines\", () -&gt; Destination.at(mines, Component.text(\"Deep Mines\")))\n        .build())\n    .build();\n</code></pre> <ol> <li>A strict level cannot be omitted: <code>/nav dungeon crypt</code> is valid; <code>/nav crypt</code> is not.</li> </ol> <p>Rules:</p> <ul> <li>Keys are single tokens. Upper case is permitted; special characters are not. Spaces are   permitted but discouraged, since keys appear in commands and permission nodes.</li> <li>Children are suppliers, evaluated only when traversed.</li> <li>Return <code>null</code> from <code>provide</code> when there are no destinations for the player. Empty trees are   omitted.</li> <li>Mark large levels <code>strict()</code>. Strict levels are skipped when computing suggestions.</li> </ul>"},{"location":"developers/destinations/#lazy-levels","title":"Lazy levels","text":"PaperSponge <pre><code>private PlatformDestinationTree&lt;World, Vector3i&gt; warps(Player player) {\n  DestinationTree tree = DestinationTree.builder().strict();\n  for (Warp warp : store.visibleTo(player)) {\n    // The lambda body runs only if this leaf is actually traversed.\n    tree.leaf(warp.name(), () -&gt; Destination.at(warp.location(), warp.name()));\n  }\n  return tree.build();\n}\n</code></pre> <pre><code>private PlatformDestinationTree&lt;ServerWorld, Vector3i&gt; warps(ServerPlayer player) {\n  DestinationTree tree = DestinationTree.builder().strict();\n  for (Warp warp : store.visibleTo(player)) {\n    // The lambda body runs only if this leaf is actually traversed.\n    tree.leaf(warp.name(), () -&gt; Destination.at(warp.location(), Component.text(warp.name())));\n  }\n  return tree.build();\n}\n</code></pre>"},{"location":"developers/destinations/#destination-types","title":"Destination types","text":"<p><code>Destination</code> provides factories for common cases:</p> PaperSponge <pre><code>// A single block.\nDestination.at(location, \"Market\");\n\n// Anywhere in one region.\nDestination.region(BoxWorldRegion.of(corner1, corner2), \"The Arena\");\nDestination.region(BoxWorldRegion.around(center, 8), \"Near the well\");\nDestination.region(WholeWorldRegion.of(world), \"The Nether\");\n\n// Several regions \u2014 the search heads for whichever is cheapest to reach, and\n// the supplier is re-evaluated every search, so a moving target stays current.\nDestination.regions(() -&gt; town.claims().stream().map(this::toRegion).toList(), \"Riverwood\");\n</code></pre> <pre><code>// A single block.\nDestination.at(location, Component.text(\"Market\"));\n\n// Anywhere in one region.\nDestination.region(BoxWorldRegion.of(corner1, corner2), Component.text(\"The Arena\"));\nDestination.region(BoxWorldRegion.around(center, 8), Component.text(\"Near the well\"));\nDestination.region(WholeWorldRegion.of(world), Component.text(\"The Nether\"));\n\n// Several regions \u2014 the search heads for whichever is cheapest to reach, and\n// the supplier is re-evaluated every search, so a moving target stays current.\nDestination.regions(\n    () -&gt; town.claims().stream().map(this::toRegion).toList(), Component.text(\"Riverwood\"));\n</code></pre> <p>A destination with no regions is valid and unreachable. Use this when the target is temporarily unavailable, such as when its world is unloaded.</p>"},{"location":"developers/destinations/#moving-targets","title":"Moving targets","text":"<p>Implement <code>MinecraftDestination</code> directly to override <code>isMobile()</code>:</p> <pre><code>public record NpcDestination(NPC npc) implements MinecraftDestination&lt;World, Vector3i&gt; {\n  @Override\n  public org.cobblestonemc.api.Destination&lt;WorldRegion&lt;World, Vector3i&gt;&gt; destination() {\n    // Re-resolved on every query, so a live trip follows the NPC.\n    return () -&gt; npc.isSpawned()\n        ? List.of(SingleCellWorldRegion.of(npc.getEntity().getLocation()))\n        : List.of();\n  }\n\n  @Override\n  public Component displayName() {\n    return Component.text(npc.getName());\n  }\n\n  @Override\n  public List&lt;String&gt; permissions() {\n    return List.of();\n  }\n\n  @Override\n  public boolean isMobile() {\n    return true;  // trips to it default to -live\n  }\n}\n</code></pre> <p><code>isMobile()</code> sets the default for live trips. Players may override it with <code>-live</code> or <code>-no-live</code>.</p>"},{"location":"developers/destinations/#permissions","title":"Permissions","text":"<ul> <li>Destination node: <code>cobblestone.navigate.&lt;address&gt;</code>. Generated automatically, default allow,   and managed by server admins.</li> <li><code>permissions()</code>: Additional required permissions, all of which the player must hold. Use for   access control only. Cobblestone's own destinations declare none.</li> </ul>"},{"location":"developers/destinations/#addressing","title":"Addressing","text":"<p>Players may omit leading words of an address if the remainder is unambiguous.</p> <ul> <li>The final key is always required.</li> <li><code>strict()</code> levels are always required.</li> <li>Keys that collide across plugins are resolved by the player using the full address.</li> <li>Matching is case-insensitive; <code>quest</code> and <code>Quest</code> collide.</li> </ul>"},{"location":"developers/navigators/","title":"Navigators","text":"<p>A navigator renders a trip. The built-in <code>trail</code> navigator draws particles along the path. Players select a navigator with <code>-navigator &lt;id&gt;</code>; plugins select one when starting a trip.</p>"},{"location":"developers/navigators/#interface","title":"Interface","text":"<pre><code>public interface Navigator&lt;L&gt; {\n  void start();                                   // the trip begins\n  void tick();                                    // every server tick: render, advance\n  void update(Path&lt;L, MinecraftStepPayload&gt; p);   // a re-search replaced the path\n  void stop();                                    // arrival, cancellation, or logout\n  boolean isComplete();                           // has the player arrived?\n  double remainingSeconds();                      // for /cobblestone trips\n\n  // Optional:\n  default boolean consumeRecalcRequest();              // \"I've lost them, re-search\"\n  default Optional&lt;L&gt; consumeGuideRequest();           // \"give me a short path back to the trail\"\n  default void setGuidePath(Path&lt;L, MinecraftStepPayload&gt; guide);\n}\n</code></pre> <p><code>tick()</code> runs every server tick on the player's scheduler (a region task on Folia). It must not block.</p> <p>The trip ends when <code>isComplete()</code> returns <code>true</code>. Arrival is determined by the navigator; the built-in trail uses a two-block radius.</p>"},{"location":"developers/navigators/#example","title":"Example","text":"<p>An action-bar compass that displays a bearing:</p> PaperSponge <pre><code>final class CompassNavigator implements Navigator&lt;Location&gt; {\n  private final Player player;\n  private Path&lt;Location, MinecraftStepPayload&gt; path;\n  private int step;\n\n  CompassNavigator(Player player, Path&lt;Location, MinecraftStepPayload&gt; path) {\n    this.player = player;\n    this.path = path;\n  }\n\n  @Override public void start() { step = 0; }\n\n  @Override public void tick() {\n    if (isComplete() || !player.isOnline()) {\n      return;\n    }\n    Location target = path.steps().get(step).position();\n    if (player.getLocation().distanceSquared(target) &lt; 4) {\n      step++;\n      return;\n    }\n    player.sendActionBar(Component.text(bearing(player.getLocation(), target)));\n  }\n\n  @Override public void update(Path&lt;Location, MinecraftStepPayload&gt; newPath) {\n    this.path = newPath;\n    this.step = 0;\n  }\n\n  @Override public void stop() {\n    player.sendActionBar(Component.empty());\n  }\n\n  @Override public boolean isComplete() {\n    return step &gt;= path.steps().size();\n  }\n\n  @Override public double remainingSeconds() {\n    double total = 0;\n    for (int i = step; i &lt; path.steps().size(); i++) {\n      total += path.steps().get(i).time();\n    }\n    return total;\n  }\n}\n</code></pre> <pre><code>final class CompassNavigator implements Navigator&lt;ServerLocation&gt; {\n  private final ServerPlayer player;\n  private Path&lt;ServerLocation, MinecraftStepPayload&gt; path;\n  private int step;\n\n  CompassNavigator(ServerPlayer player, Path&lt;ServerLocation, MinecraftStepPayload&gt; path) {\n    this.player = player;\n    this.path = path;\n  }\n\n  @Override public void start() { step = 0; }\n\n  @Override public void tick() {\n    if (isComplete() || !player.isOnline()) {\n      return;\n    }\n    ServerLocation target = path.steps().get(step).position();\n    if (player.position().distanceSquared(target.position()) &lt; 4) {\n      step++;\n      return;\n    }\n    player.sendActionBar(Component.text(bearing(player.serverLocation(), target)));\n  }\n\n  @Override public void update(Path&lt;ServerLocation, MinecraftStepPayload&gt; newPath) {\n    this.path = newPath;\n    this.step = 0;\n  }\n\n  @Override public void stop() {\n    player.sendActionBar(Component.empty());\n  }\n\n  @Override public boolean isComplete() {\n    return step &gt;= path.steps().size();\n  }\n\n  @Override public double remainingSeconds() {\n    double total = 0;\n    for (int i = step; i &lt; path.steps().size(); i++) {\n      total += path.steps().get(i).time();\n    }\n    return total;\n  }\n}\n</code></pre>"},{"location":"developers/navigators/#registration","title":"Registration","text":"PaperSponge <pre><code>CobblestonePaperApi.registrar().registerNavigator(this, \"compass\",\n    (player, path, settings) -&gt; new CompassNavigator(player, path));\n</code></pre> <pre><code>CobblestoneSpongeApi.registrar().registerNavigator(container, \"compass\",\n    (player, path, settings) -&gt; new CompassNavigator(player, path));\n</code></pre> <p>Ids are lower-case. The first registration of an id takes precedence. The navigator is gated by <code>cobblestone.navigator.&lt;id&gt;</code> (default allow) and removed when the owner disables.</p>"},{"location":"developers/navigators/#settings","title":"Settings","text":"<p><code>NavigatorSettings</code> carries per-trip overrides. Declare typed keys for the settings your navigator supports:</p> <pre><code>public final class CompassSettings {\n  public static final String NAVIGATOR_ID = \"compass\";\n\n  public static final NavigatorSettingKey&lt;Boolean&gt; SHOW_DISTANCE =\n      new NavigatorSettingKey&lt;&gt;(\"compass.show_distance\");\n\n  public static NavigatorSettings showingDistance(boolean show) {\n    return NavigatorSettings.builder(NAVIGATOR_ID).set(SHOW_DISTANCE, show).build();\n  }\n}\n</code></pre> <p>Read them in the factory, falling back to configuration when unset:</p> <pre><code>boolean showDistance = settings.get(CompassSettings.SHOW_DISTANCE).orElse(config.showDistance());\n</code></pre>"},{"location":"developers/navigators/#reading-the-path","title":"Reading the path","text":"<p>A <code>Path</code> is an origin and an ordered list of <code>Step</code>s. Each step has a position, <code>cost</code> and <code>time</code> in seconds, and a <code>MinecraftStepPayload</code>:</p> <pre><code>for (Step&lt;Location, MinecraftStepPayload&gt; step : path.steps()) {\n  MinecraftStepType type = step.payload().stepType();   // WALK, SWIM, FALL, BOAT, TELEPORT\u2026\n  MinecraftInstruction instruction = step.payload().instruction();\n}\n</code></pre> <p><code>path.duration()</code> is the estimated travel time; <code>path.cost()</code> is the value minimized by the search.</p>"},{"location":"developers/navigators/#action-steps","title":"Action steps","text":"<p><code>OPEN_DOOR</code>, <code>PLACE_BOAT</code>, <code>MOUNT_HORSE</code>, and <code>TELEPORT</code> require player action. <code>MinecraftStepType#isAction()</code> identifies them. A step may also carry an instruction:</p> <pre><code>if (step.payload().instruction()\n    instanceof MinecraftInstruction.CommandInstruction(String command)) {\n  // e.g. \"/warp market\" \u2014 make it clickable rather than making them type it.\n  player.sendMessage(Component.text(\"Run \" + command)\n      .clickEvent(ClickEvent.runCommand(command)));\n}\n</code></pre> <p>Navigators should notify the player at action steps.</p>"},{"location":"developers/navigators/#straying","title":"Straying","text":"<p>Two optional hooks handle players who leave the route:</p> <ul> <li><code>consumeRecalcRequest()</code>: Return <code>true</code> to re-search from the player's position. The new   path is passed to <code>update</code>.</li> <li><code>consumeGuideRequest()</code>: Return a target to compute a short path from the player to it. The   result is passed to <code>setGuidePath</code>.</li> </ul> <p>Both default to no request.</p>"},{"location":"developers/search-modification/","title":"Search modification","text":"<p>A <code>SearchModificationService</code> modifies searches through three optional methods:</p> Method Purpose <code>computeTransitions</code> Adds edges, such as teleports. <code>computeBreakChecker</code> Restricts which blocks may be broken. <code>computePassChecker</code> Restricts which cells may be entered. <p>Registered services apply to every search:</p> PaperSponge <pre><code>import org.cobblestonemc.paper.api.CobblestoneCoreApi;\n\nCobblestoneCoreApi.registrar().register(this, new WarpModifications(store));\n</code></pre> <pre><code>import org.cobblestonemc.sponge.api.CobblestoneCoreApi;\n\nCobblestoneCoreApi.registrar().register(container, new WarpModifications(store));\n</code></pre> <p>The registration is removed when the owner disables.</p> <p>The <code>compute*</code> methods run once per search on the calling thread, normally the main thread. The returned checkers run during the search, possibly off the main thread. See Threading.</p>"},{"location":"developers/search-modification/#transitions","title":"Transitions","text":"<p>A <code>Transition</code> is an edge from any cell in an origin region to a destination location, at a given cost.</p> <pre><code>Transition.of(origin, destination, costSeconds, payload);\nTransition.of(origin, destination, costSeconds, timeSeconds, payload);\nTransition.command(player, destination, costSeconds, \"/warp market\");\n</code></pre> <ul> <li><code>origin</code>: A <code>WorldRegion</code>: <code>SingleCellWorldRegion.of(loc)</code>, <code>BoxWorldRegion.of(a, b)</code>,   <code>BoxWorldRegion.around(center, radius)</code>, or <code>WholeWorldRegion.of(world)</code>.</li> <li><code>cost</code>: The value minimized by the search, in seconds. <code>time</code>: The travel time reported to   the player. Set <code>cost</code> above <code>time</code> to discourage a transition without misreporting its duration.</li> <li><code>payload</code>: The step type: <code>MinecraftStepPayload.portal()</code>,   <code>MinecraftStepPayload.command(\"/warp market\")</code>, or <code>MinecraftStepPayload.of(type)</code>.</li> </ul>"},{"location":"developers/search-modification/#command-transitions","title":"Command transitions","text":"<p>A command can be run from anywhere, so its origin is the player's entire world:</p> PaperSponge <pre><code>@Override\npublic CompletableFuture&lt;List&lt;Transition&gt;&gt; computeTransitions(Player player) {\n  List&lt;Transition&gt; transitions = new ArrayList&lt;&gt;();\n  for (Warp warp : store.warps()) {\n    if (!player.hasPermission(\"warps.use.\" + warp.name())) {\n      continue;                                   // (1)!\n    }\n    transitions.add(Transition.command(\n        player, warp.location(), 3.0, \"/warp \" + warp.name()));\n  }\n  return CompletableFuture.completedFuture(transitions);\n}\n</code></pre> <ol> <li>Offer a teleport only if the player may use it. Cobblestone does not check teleport    permissions.</li> </ol> <pre><code>@Override\npublic CompletableFuture&lt;List&lt;Transition&gt;&gt; computeTransitions(ServerPlayer player) {\n  List&lt;Transition&gt; transitions = new ArrayList&lt;&gt;();\n  for (Warp warp : store.warps()) {\n    if (!player.hasPermission(\"warps.use.\" + warp.name())) {\n      continue;                                   // (1)!\n    }\n    transitions.add(Transition.command(\n        player, warp.location(), 3.0, \"/warp \" + warp.name()));\n  }\n  return CompletableFuture.completedFuture(transitions);\n}\n</code></pre> <ol> <li>Offer a teleport only if the player may use it. Cobblestone does not check teleport    permissions.</li> </ol>"},{"location":"developers/search-modification/#portal-transitions","title":"Portal transitions","text":"<p>A portal pad teleports players on entry, so its origin is the pad's bounding box:</p> PaperSponge <pre><code>BoxWorldRegion pad = BoxWorldRegion.of(padCorner1, padCorner2);\ntransitions.add(Transition.of(pad, exitLocation, 1.0, MinecraftStepPayload.portal()));\n</code></pre> <pre><code>BoxWorldRegion pad = BoxWorldRegion.of(padCorner1, padCorner2);\ntransitions.add(Transition.of(pad, exitLocation, 1.0, MinecraftStepPayload.portal()));\n</code></pre> <p>Omit transitions that are currently unusable, such as those into unloaded worlds.</p>"},{"location":"developers/search-modification/#break-checkers","title":"Break checkers","text":"<p>Routes may mine through blocks. A break checker restricts which blocks may be broken; the Towny integration uses one to enforce build protection.</p> PaperSponge <pre><code>@Override\npublic BreakChecker computeBreakChecker(Player player) {\n  // Called once per search: capture what you need here, on the main thread.\n  RegionQuery query = worldGuard.createQuery();\n  return (p, location, block) -&gt; {\n    // Runs during the search, possibly off-thread. Answer without blocking.\n    return CompletableFuture.completedFuture(query.testBuild(location, p));\n  };\n}\n</code></pre> <p>The block is supplied lazily (<code>Supplier&lt;BlockData&gt;</code>). Call it only if needed; it may require a chunk lookup.</p> <pre><code>@Override\npublic BreakChecker computeBreakChecker(ServerPlayer player) {\n  // Called once per search: capture what you need here, on the main thread.\n  var claims = claimService.snapshotFor(player);\n  return (p, location, block) -&gt; {\n    // Runs during the search, possibly off-thread. Answer without blocking.\n    return CompletableFuture.completedFuture(claims.mayBreak(location, block));\n  };\n}\n</code></pre> <p>If the check requires the main thread, schedule it and complete the future there:</p> <pre><code>return (p, location, block) -&gt; {\n  CompletableFuture&lt;Boolean&gt; answer = new CompletableFuture&lt;&gt;();\n  scheduler.runOnMainThread(() -&gt; answer.complete(protection.canDestroy(p, location)));\n  return answer;\n};\n</code></pre> <p>A search may invoke the checker thousands of times. Cache results per search.</p>"},{"location":"developers/search-modification/#pass-checkers","title":"Pass checkers","text":"<p>A pass checker restricts which cells the player may enter.</p> PaperSponge <pre><code>@Override\npublic PassChecker computePassChecker(Player player) {\n  boolean vip = player.hasPermission(\"myserver.vip\");\n  return (p, location) -&gt;\n      CompletableFuture.completedFuture(vip || !vipArea.contains(location));\n}\n</code></pre> <pre><code>@Override\npublic PassChecker computePassChecker(ServerPlayer player) {\n  boolean vip = player.hasPermission(\"myserver.vip\");\n  return (p, location) -&gt;\n      CompletableFuture.completedFuture(vip || !vipArea.contains(location));\n}\n</code></pre> <p>Searches do not route through denied cells.</p> <p><code>BreakChecker.ALLOW</code> and <code>PassChecker.ALLOW</code> are the defaults.</p>"},{"location":"developers/search-modification/#example-plugin","title":"Example plugin","text":"<p><code>examples/paper-warps</code> implements <code>/warp</code> commands and portal pads with a single <code>SearchModificationService</code>, using only the published API.</p>"},{"location":"developers/searching/","title":"Searching","text":"<p><code>NavigationService</code> computes a path from a player to a destination. Use it to measure distance, test reachability, or render a path yourself. To guide a player, use Trips instead.</p>"},{"location":"developers/searching/#running-a-search","title":"Running a search","text":"PaperSponge <pre><code>import org.cobblestonemc.api.NavigationResult;\nimport org.cobblestonemc.api.SearchHandle;\nimport org.cobblestonemc.minecraft.api.MinecraftStepPayload;\nimport org.cobblestonemc.paper.api.CobblestoneCoreApi;\n\nSearchHandle&lt;Location, MinecraftStepPayload&gt; handle =\n    CobblestoneCoreApi.navigationService().navigatePlayer(player, destination);\n\nhandle.future().thenAccept(result -&gt; {\n  switch (result) {\n    case NavigationResult.Success&lt;Location, MinecraftStepPayload&gt; success -&gt; {\n      Path&lt;Location, MinecraftStepPayload&gt; path = success.path();\n      log(\"{} steps, about {}s\", path.steps().size(), Math.round(path.duration()));\n    }\n    case NavigationResult.Failure&lt;Location, MinecraftStepPayload&gt; failure -&gt;\n        log(\"no path: {}\", failure.reason());\n    case NavigationResult.Error&lt;Location, MinecraftStepPayload&gt; error -&gt;\n        log.warn(\"search failed\", error.throwable());\n  }\n});\n</code></pre> <pre><code>import org.cobblestonemc.api.NavigationResult;\nimport org.cobblestonemc.api.SearchHandle;\nimport org.cobblestonemc.minecraft.api.MinecraftStepPayload;\nimport org.cobblestonemc.sponge.api.CobblestoneCoreApi;\n\nSearchHandle&lt;ServerLocation, MinecraftStepPayload&gt; handle =\n    CobblestoneCoreApi.navigationService().navigatePlayer(player, destination);\n\nhandle.future().thenAccept(result -&gt; {\n  switch (result) {\n    case NavigationResult.Success&lt;ServerLocation, MinecraftStepPayload&gt; success -&gt; {\n      Path&lt;ServerLocation, MinecraftStepPayload&gt; path = success.path();\n      log(\"{} steps, about {}s\", path.steps().size(), Math.round(path.duration()));\n    }\n    case NavigationResult.Failure&lt;ServerLocation, MinecraftStepPayload&gt; failure -&gt;\n        log(\"no path: {}\", failure.reason());\n    case NavigationResult.Error&lt;ServerLocation, MinecraftStepPayload&gt; error -&gt;\n        log.warn(\"search failed\", error.throwable());\n  }\n});\n</code></pre> <p>Searches always originate at a player, since traversability depends on the player's world, permissions, and abilities.</p>"},{"location":"developers/searching/#regions","title":"Regions","text":"<p>To target any cell within a box, pass two opposite corners:</p> <pre><code>navigationService.navigatePlayerToRegion(player, corner1, corner2);\n</code></pre> <p>The search targets the cheapest reachable cell in the box.</p>"},{"location":"developers/searching/#cancellation","title":"Cancellation","text":"<pre><code>handle.cancel();\n</code></pre> <p>Idempotent. If the search is still running, the future completes with <code>FailureReason.CANCELLED</code>. Cancel searches that are no longer needed.</p>"},{"location":"developers/searching/#settings","title":"Settings","text":"<p><code>MinecraftSearchSettings</code> combines algorithm limits with Minecraft-specific exclusions:</p> <pre><code>MinecraftSearchSettings settings = new MinecraftSearchSettings(\n    SearchSettings.builder()\n        .maxCellsVisited(50_000)      // cheaper than the server default\n        .maxWallClockMillis(5_000L)\n        .heuristicWeight(2.0)         // faster, slightly suboptimal\n        .build(),\n    Set.of(MinecraftStepType.MINE),   // don't route through digging\n    Set.of(),                         // excluded world keys\n    Set.of(\"the_nether\"));            // excluded dimensions\n\nnavigationService.navigatePlayer(player, destination, settings);\n</code></pre> <p><code>MinecraftSearchSettings.defaults()</code> uses library defaults, not the values in <code>config.yml</code>. Short queries, such as reachability checks, should set smaller limits.</p> Setting Default Meaning <code>maxCellsVisited</code> 200 000 Cell limit; a few hundred bytes per cell <code>maxWallClockMillis</code> 60 000 Time limit <code>heuristicWeight</code> 1.5 <code>1.0</code> is optimal; higher is faster and bounded-suboptimal <code>tier1UnsolvedPessimism</code> 1.5 Cost multiplier for unsolved route legs <code>runningAverageWidth</code> 5 Heuristic smoothing window"},{"location":"developers/searching/#paths","title":"Paths","text":"<pre><code>record Path&lt;P, T&gt;(P origin, List&lt;Step&lt;P, T&gt;&gt; steps) { }\nrecord Step&lt;P, T&gt;(P position, double cost, double time, T payload) { }\n</code></pre> <ul> <li><code>origin</code>: The player's position when the search ran.</li> <li><code>steps</code>: Positions in order. The last is the destination.</li> <li><code>cost</code>, <code>time</code>: Per step, not cumulative. <code>path.cost()</code> and <code>path.duration()</code> compute sums;   cache the results.</li> <li><code>payload</code>: A <code>MinecraftStepPayload</code> containing the <code>MinecraftStepType</code> (<code>WALK</code>, <code>SWIM</code>, <code>FLY</code>,   <code>MINE</code>, <code>FALL</code>, <code>CLIMB</code>, <code>BOAT</code>, <code>HORSE</code>, <code>OPEN_DOOR</code>, <code>PLACE_BOAT</code>, <code>MOUNT_HORSE</code>, <code>TELEPORT</code>)   and an optional <code>MinecraftInstruction</code>.</li> </ul> <p>Paths may cross worlds at <code>TELEPORT</code> steps.</p>"},{"location":"developers/searching/#threading","title":"Threading","text":"<ul> <li>Call <code>navigatePlayer</code> from the server thread. It returns immediately.</li> <li>The future completes on a Cobblestone worker thread.</li> <li>World data is read on the threads the platform requires; no preloading is needed.</li> </ul>"},{"location":"developers/searching/#failure-reasons","title":"Failure reasons","text":"Reason Meaning <code>NO_ROUTE</code> No route connects the origin to the destination. <code>DESTINATION_UNREACHABLE</code> The destination itself is not reachable. <code>LIMIT_EXCEEDED</code> The cell limit was reached. <code>TIMED_OUT</code> The time limit was reached. <code>CANCELLED</code> <code>cancel()</code> was called, or the player disconnected. <p><code>LIMIT_EXCEEDED</code> and <code>TIMED_OUT</code> indicate that no route was found within the limits, not that none exists.</p>"},{"location":"developers/trips/","title":"Trips","text":"<p>A trip is a path being followed by a player, rendered by a navigator. Trips recompute when the player strays and end on arrival. <code>TripService</code> starts trips programmatically.</p>"},{"location":"developers/trips/#starting-a-trip","title":"Starting a trip","text":"PaperSponge <pre><code>import org.cobblestonemc.paper.plugin.api.CobblestonePaperApi;\nimport org.cobblestonemc.plugin.api.NavigatorSettings;\n\nCobblestonePaperApi.tripService()\n    .navigate(player, objectiveLocation, NavigatorSettings.defaults(), \"Lost Lantern\");\n</code></pre> <pre><code>import org.cobblestonemc.sponge.plugin.api.CobblestoneSpongeApi;\nimport org.cobblestonemc.plugin.api.NavigatorSettings;\n\nCobblestoneSpongeApi.tripService()\n    .navigate(player, objectiveLocation, NavigatorSettings.defaults(), \"Lost Lantern\");\n</code></pre> <p>This runs the search and starts the trip. The label is displayed in <code>/cobblestone trips</code>.</p>"},{"location":"developers/trips/#outcomes","title":"Outcomes","text":"<p><code>navigate</code> returns a <code>CompletableFuture&lt;TripOutcome&gt;</code>:</p> <pre><code>tripService.navigate(player, objective, NavigatorSettings.defaults(), \"Lost Lantern\")\n    .thenAccept(outcome -&gt; {\n      switch (outcome) {\n        case TripOutcome.Started started -&gt;\n            log(\"trip {} started, about {}s\", started.tripId(), started.durationSeconds());\n        case TripOutcome.Failed failed -&gt;\n            player.sendMessage(\"No route found.\");   // failed.reason()\n        case TripOutcome.TripLimitReached ignored -&gt;\n            player.sendMessage(\"Finish or cancel a trip first.\");\n        case TripOutcome.Error error -&gt;\n            log.warn(\"navigation failed\", error.throwable());\n      }\n    });\n</code></pre> <p><code>FailureReason</code> is one of <code>NO_ROUTE</code>, <code>DESTINATION_UNREACHABLE</code>, <code>LIMIT_EXCEEDED</code>, <code>TIMED_OUT</code>, <code>CANCELLED</code>.</p> <p>An overload accepts only a failure callback:</p> <pre><code>tripService.navigate(player, objective, NavigatorSettings.defaults(),\n    reason -&gt; player.sendMessage(\"No route: \" + reason));\n</code></pre> <p>Warning</p> <p>The future completes off the main thread. Return to the server or region thread before accessing server state.</p>"},{"location":"developers/trips/#navigator-settings","title":"Navigator settings","text":"<p><code>NavigatorSettings</code> selects the navigator and overrides its appearance for a single trip. Unset values fall back to the server configuration.</p> PaperSponge <pre><code>import org.bukkit.Color;\nimport org.bukkit.Particle;\nimport org.cobblestonemc.paper.plugin.api.TrailNavigatorSettings;\n\nNavigatorSettings settings = TrailNavigatorSettings.builder()\n    .particles(List.of(Particle.DUST))\n    .colors(List.of(Color.fromRGB(0xFFD54F), Color.fromRGB(0x4FC3F7)))\n    .highlightParticles(List.of(Particle.END_ROD))\n    .build();\n\ntripService.navigate(player, objective, settings, \"Lost Lantern\");\n</code></pre> <pre><code>import org.spongepowered.api.effect.particle.ParticleTypes;\nimport org.spongepowered.api.util.Color;\nimport org.cobblestonemc.sponge.plugin.api.TrailNavigatorSettings;\n\nNavigatorSettings settings = TrailNavigatorSettings.builder()\n    .particles(List.of(ParticleTypes.DUST.get()))\n    .colors(List.of(Color.ofRgb(0xFFD54F), Color.ofRgb(0x4FC3F7)))\n    .highlightParticles(List.of(ParticleTypes.END_ROD.get()))\n    .build();\n\ntripService.navigate(player, objective, settings, \"Lost Lantern\");\n</code></pre> <p>To use another navigator, build settings for its id:</p> <pre><code>NavigatorSettings.builder(\"guide\").build();\n</code></pre> <p><code>NavigatorSettings.defaults()</code> selects the server's default navigator with its configured appearance.</p>"},{"location":"developers/trips/#starting-a-trip-from-a-path","title":"Starting a trip from a path","text":"<p>To render a path from a search you ran (see Searching):</p> PaperSponge <pre><code>SearchHandle&lt;Location, MinecraftStepPayload&gt; handle =\n    CobblestoneCoreApi.navigationService().navigatePlayer(player, destination);\n\nhandle.future().thenAccept(result -&gt; {\n  if (result instanceof NavigationResult.Success&lt;Location, MinecraftStepPayload&gt; success) {\n    CobblestonePaperApi.tripService()\n        .startTrip(player, success.path(), NavigatorSettings.defaults());\n  }\n});\n</code></pre> <pre><code>SearchHandle&lt;ServerLocation, MinecraftStepPayload&gt; handle =\n    CobblestoneCoreApi.navigationService().navigatePlayer(player, destination);\n\nhandle.future().thenAccept(result -&gt; {\n  if (result instanceof NavigationResult.Success&lt;ServerLocation, MinecraftStepPayload&gt; success) {\n    CobblestoneSpongeApi.tripService()\n        .startTrip(player, success.path(), NavigatorSettings.defaults());\n  }\n});\n</code></pre> <p>Trips started this way are not live. Use <code>navigate</code> for trips that re-search.</p>"},{"location":"developers/trips/#lifecycle","title":"Lifecycle","text":"<p>Programmatic trips appear in <code>/cobblestone trips</code>, count toward <code>trips.max_active_per_player</code>, and can be cancelled by the player. Do not restart a trip the player has cancelled.</p> <p>Trips end on arrival, cancellation, or disconnect.</p>"}]}