// core-test — pure-Java test engine (fake worlds/modes) + the algorithm tests. Not published. (design/09)
plugins {
    id("odyssey.java-conventions")
}

dependencies {
    implementation(project(":core"))
}
