# Version catalog for module-level dependencies.
# Build-plugin versions (checkstyle tool, spotless) are pinned in buildSrc/ where the convention
# plugins live. External platform/library deps (adventure, paper-api, spongeapi, JavaFX, JDBC
# drivers, config lib, bStats, prometheus) get added here in their respective implementation phases.

[versions]
junit = "5.12.1"

[libraries]
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher" }
