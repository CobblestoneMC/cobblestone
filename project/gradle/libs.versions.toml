# Version catalog for module-level dependencies.
# Build-plugin versions (spotless) are pinned in buildSrc/ where the convention
# plugins live. External platform/library deps are added here in their respective implementation
# phases.

[versions]
junit = "5.12.1"
# Kyori Adventure enters at the plugin-api layer (rich Component messages). Provided at runtime by
# Paper/Sponge, so it is only ever compileOnly.
adventure = "4.17.0"
# Paper server API (compileOnly; provided by the server). Also bundles Adventure at runtime.
paper = "26.1.2.build.+"
# SpongeAPI (compileOnly; provided by the server). Floor API 12 — one jar serves API 12–17. Also
# bundles Adventure and Configurate at runtime.
sponge = "12.0.0"
# YAML parser for the platform-neutral ConfigManager. Resolved at runtime by the paper-plugin loader
# (Paper's MavenLibraryResolver), so it stays compileOnly at compile time.
snakeyaml = "2.3"
# GradleUp shadow — bundles only our own net.whimxiqal.odyssey.* modules into the shipped plugin jar.
shadow = "9.0.0"
# JDBC drivers for the embedded DataStore backends.
h2 = "2.2.224"
# bStats — anonymous usage metrics. Shaded into the plugin jar and relocated (design/10) so it never
# clashes with another plugin's copy.
bstats = "3.2.1"

[libraries]
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher" }
adventure-api = { module = "net.kyori:adventure-api", version.ref = "adventure" }
paper-api = { module = "io.papermc.paper:paper-api", version.ref = "paper" }
spongeapi = { module = "org.spongepowered:spongeapi", version.ref = "sponge" }
snakeyaml = { module = "org.yaml:snakeyaml", version.ref = "snakeyaml" }
h2 = { module = "com.h2database:h2", version.ref = "h2" }
bstats-base = { module = "org.bstats:bstats-base", version.ref = "bstats" }
bstats-bukkit = { module = "org.bstats:bstats-bukkit", version.ref = "bstats" }
bstats-sponge = { module = "org.bstats:bstats-sponge", version.ref = "bstats" }

[plugins]
shadow = { id = "com.gradleup.shadow", version.ref = "shadow" }
